[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Build command to install Deno and dependencies
[nixpacks]
aptPkgs = ["ffmpeg", "deno", "unzip"]
